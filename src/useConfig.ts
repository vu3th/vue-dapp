import { ref } from 'vue-demi'

const appChainId: number = 0

// supported
const supportedChainIds: number[] = []
const supportedWallets: string[] = ['metamask', 'walletconnect']

export function useConfig() {}
